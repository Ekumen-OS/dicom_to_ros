services:
  dicom_listener:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: dicom_ros_image
    container_name: dicom_listener

    ports:
      - "11112:11112"
    
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1 
      
    volumes:
      - ./src:/ros2_ws/src
      - /tmp/.X11-unix:/tmp/.X11-unix:rw 
      
    command: /bin/bash -c "rm -rf build install log && source /opt/ros/humble/setup.bash && colcon build --symlink-install && source install/setup.bash && ros2 run dicom_to_ros dicom_listener"